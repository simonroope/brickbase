{
  "name": "chains",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "contracts/chains",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint contracts/chains --max-warnings 0"
      }
    }
  },
  "tags": ["scope:shared"]
}
