{
  "name": "abi",
  "$schema": "../../../node_modules/nx/schemas/project-schema.json",
  "projectType": "library",
  "sourceRoot": "contracts/abi/src",
  "targets": {
    "lint": {
      "executor": "nx:run-commands",
      "options": {
        "command": "npx eslint contracts/abi --max-warnings 0"
      }
    },
    "build": {
      "executor": "nx:run-commands",
      "options": {
        "command": "echo 'ABI lib - no build step, reads from contracts artifacts'"
      }
    }
  },
  "implicitDependencies": ["contracts"],
  "tags": ["scope:shared"]
}
