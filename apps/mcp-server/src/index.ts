/**
 * Brickbase MCP Server
 * Exposes smart contracts, config, and (future) databases/APIs via Model Context Protocol.
 *
 * To implement: install @modelcontextprotocol/sdk and add tools/resources per PRD.
 * Tools: get_property_list, get_property_detail, get_oracle_prices, get_user_whitelist_status,
 *        get_user_shares, purchase_shares, set_user_allowed
 * Resources: contract://AssetVault/abi, contract://AssetShares/abi, contract://OracleRouter/abi,
 *            config://deployments, config://chains
 */
console.log("Brickbase MCP server - scaffold ready. Add @modelcontextprotocol/sdk to implement.");
